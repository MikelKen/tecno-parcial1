spring.application.name=tecno_proyect

# Configuración del servidor web
server.port=8081

# Configuración de base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://www.tecnoweb.org.bo:5432/db_grupo03sa
spring.datasource.username=grupo03sa
spring.datasource.password=grup003grup003*
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración de logging
logging.level.com.example.tecno_proyect=DEBUG
logging.level.org.springframework.mail=DEBUG
